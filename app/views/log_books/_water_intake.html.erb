<div id="toast-water-intake" class="flex items-center w-full max-w-xs p-4 text-gray-500 bg-white rounded-lg shadow dark:text-gray-400 dark:bg-gray-800" role="alert">
  <div class="inline-flex items-center justify-center flex-shrink-0 w-8 h-8 text-blue-500 bg-blue-100 rounded-lg dark:bg-blue-800 dark:text-blue-200">
    <%= image_tag("/assets/watering-plants.png", width: "24", height: "24", alt: "emoji arrosoir") %>
  </div>

  <div class="ml-3 text-sm font-normal">
    <% if @owned_plant.plant.water_requirement_level == "Faible" %>
      <% if @log_books.last.entry_date < 7.days.ago.to_date %>
        <h1>Pensez bien à arroser votre plante, et à nous tenir informé via votre journal de bord !</h1>
      <% elsif @log_books.last.watered == true && @log_books.last.entry_date > 7.days.ago.to_date %>
        <h1>Selon nos informations, vous avez récemment arrosé votre plante : elle est en pleine forme :D </h1>
      <% elsif @log_books.last.watered == false && @log_books.last.entry_date > 7.days.ago.to_date %>
        <h1>Votre plante a besoin d'un peu d'eau ! N'oubliez pas de l'hydrater, et de le noter dans une nouvelle entrée sur votre journal de bord.</h1>
      <% else %>
        <h1>Suivant les données que nous avons, votre plante n'a pas besoin d'eau aujourd'hui. Nous vous conseillons cependant d'y jeter un coup d'oeil tous les jours !</h1>
      <% end %>
    <% end %>

    <% if @owned_plant.plant.water_requirement_level == "Moyen" %>
      <% if @log_books.last.entry_date < 5.days.ago.to_date %>
        <h1>Pensez bien à arroser votre plante, et à nous tenir informé via votre journal de bord !</h1>
      <% elsif @log_books.last.watered == true && @log_books.last.entry_date > 5.days.ago.to_date %>
        <h1>Selon nos informations, vous avez récemment arrosé votre plante : elle est en pleine forme :D </h1>
      <% elsif @log_books.last.watered == false && @log_books.last.entry_date > 5.days.ago.to_date %>
        <h1>Votre plante a besoin d'un peu d'eau ! N'oubliez pas de l'hydrater, et de le noter dans une nouvelle entrée sur votre journal de bord.</h1>
      <% else %>
        <h1>Suivant les données que nous avons, votre plante n'a pas besoin d'eau aujourd'hui. Nous vous conseillons cependant d'y jeter un coup d'oeil tous les jours !</h1>
      <% end %>
    <% end %>

    <% if @owned_plant.plant.water_requirement_level == "Important" %>
      <% if @log_books.last.entry_date < 3.days.ago.to_date %>
        <h1>Pensez bien à arroser votre plante, et à nous tenir informé via votre journal de bord !</h1>
      <% elsif @log_books.last.watered == true && @log_books.last.entry_date > 3.days.ago.to_date %>
        <h1>Selon nos informations, vous avez récemment arrosé votre plante : elle est en pleine forme :D </h1>
      <% elsif @log_books.last.watered == false && @log_books.last.entry_date > 3.days.ago.to_date %>
        <h1>Votre plante a besoin d'un peu d'eau ! N'oubliez pas de l'hydrater, et de le noter dans une nouvelle entrée sur votre journal de bord.</h1>
      <% else %>
        <h1>Suivant les données que nous avons, votre plante n'a pas besoin d'eau aujourd'hui. Nous vous conseillons cependant d'y jeter un coup d'oeil tous les jours !</h1>
      <% end %>
    <% end %>
  </div>

  <button type="button" class="ml-auto -mx-1.5 -my-1.5 bg-white text-gray-400 hover:text-gray-900 rounded-lg focus:ring-2 focus:ring-gray-300 p-1.5 hover:bg-gray-100 inline-flex h-8 w-8 dark:text-gray-500 dark:hover:text-white dark:bg-gray-800 dark:hover:bg-gray-700" data-dismiss-target="#toast-water-intake" aria-label="Close">
    <span class="sr-only">Close</span>
    <svg aria-hidden="true" class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path></svg>
  </button>
</div>