
<div class="dashboard-container min-h-screen">
  <% if notice.present? %>
  <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

<div class="px-4 py-16 mx-auto sm:max-w-xl md:max-w-full lg:max-w-screen-xl md:px-24 lg:px-8 lg:py-20">
  <div class="max-w-xl sm:mx-auto lg:max-w-2xl">
    <div class="flex flex-col sm:text-center sm:mb-0">
      <a href="/" class="mb-6 sm:mx-auto">
        <div class="flex items-center justify-center rounded-full w-48 h-48 bg-indigo-50">
          <img src="<%= asset_path('homeplant.jpg') %>" alt="communiterra logo" class="object-contain rounded-full">
        </div>
      </a>
      <div class="max-w-xl mb-10 md:mx-auto sm:text-center lg:max-w-2xl md:mb-12">
        <h2 class="max-w-lg mb-6 font-sans text-3xl font-bold leading-none tracking-tight text-gray-900 sm:text-4xl md:mx-auto">
          <span class="relative inline-block">
            <svg viewBox="0 0 52 24" fill="currentColor" class="absolute top-0 left-0 z-0 hidden w-32 -mt-8 -ml-20 text-blue-gray-100 lg:w-32 lg:-ml-28 lg:-mt-10 sm:block">
              <defs>
                <pattern id="e77df901-b9d7-4b9b-822e-16b2d410795b" x="0" y="0" width=".135" height=".30">
                  <circle cx="1" cy="1" r=".7"></circle>
                </pattern>
              </defs>
            </svg>
            <span class="relative">Bienvenue</span>
          </span>
          sur votre jardin virtuel
        </h2>
        <p class="text-base text-gray-700 md:text-lg">
          Vous pouvez ici visualiser toutes vos plantes, écrire des nouvelles entrées dans les journaux de bords, et modifier votre profil. 
        </p>
      </div>
    </div>
  </div>

  <div class="px-4 py-16 mx-auto sm:max-w-xl md:max-w-full lg:max-w-screen-xl md:px-24 lg:px-8 lg:py-20">
    <div class="px-4 mb-8 mx-auto">
      <div class="grid grid-cols-2 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-2 gap-4">
        <div class="overflow-hidden transition-shadow duration-300 bg-white rounded shadow-sm">
          <img src="<%= asset_path('userprofile.jpg') %>" class="object-cover w-full h-64 rounded" alt="plantes d'intérieur en pot" />
          <div class="p-5 rounded border border-t-0 shadow-sm">
            <p class="mb-3 text-xs font-semibold tracking-wide uppercase">
              <span class="text-gray-600">Votre profil :</span>
            </p>
              <p class="mb-2 text-gray-700">
                <p><strong>Prénom :</strong> <%= current_user.first_name %></p>
                <p><strong>Nom :</strong> <%= current_user.last_name %></p>
                <p><strong>Email :</strong> <%= current_user.email %></p>
                <p><strong>Pseudo :</strong> <%= current_user.username %></p>
                <p><strong>Téléphone :</strong> <%= current_user.phone_number %></p>
                <p><strong>Anniversaire :</strong> <%= current_user.birthdate %></p>
                <p><strong>Bio :</strong> <%= current_user.bio %></p>
              </p>

              <div class="mt-4 flex justify-center">
                <%= link_to "Modifier mon profil", edit_user_registration_path, class: "mt-2 flex justify-center w-full rounded-md bg-green-secondary text-green-dark px-3 py-2 text-sm font-semibold leading-5 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 hover:bg-gray-pale" %>
              </div>
            </div>
          </div>
        </div>
      </div>
      <hr>

      <div class="my-4">
        <h1 class="font-semibold text-2xl">Vos plantes</h1>
      </div>

      <%= render "owned_plants/owned_plant" %>
    </div>
  </div>
</div>